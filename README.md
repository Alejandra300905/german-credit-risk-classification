# german-credit-risk-classification
This repository contains a credit risk classification project developed in Python using Jupyter Notebook. It compares Logistic Regression, Decision Tree, Random Forest, and SVM models using the German Credit dataset. The project includes data exploration, preprocessing, model evaluation, and hyperparameter tuning with GridSearchCV.
